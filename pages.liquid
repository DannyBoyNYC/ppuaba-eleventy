---
pagination:
  data: contentful
  size: 1
  alias: page
  addAllPagesToCollections: true
permalink: "pages/{{ page.slug | slug }}/"
tags: "page"
layout: layout.liquid

eleventyComputed:
  title: "{{ page.title}}"
---

<h1>
  {{ page.title }}
</h1>

<time>{{ page.date }}</time>

{{ page.body }}
